requests>=2.31
# The [cbc] extra is the solver binary. PuLP 3.3 still ships a CBC inside
# the wheel; 4.0 does not. Without the extra the solver simply vanishes on
# the next major — which is how this broke once already.
pulp[cbc]>=3.0
PyYAML>=6.0
joblib>=1.3
pandas>=2.0
scikit-learn>=1.4
pytest
